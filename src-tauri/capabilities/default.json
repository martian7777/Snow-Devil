{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main shell webview only. Browser webviews (browser-*) must NOT match any capability.",
  "webviews": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "log:default",
    "core:event:default",
    "core:event:allow-emit",
    "core:event:allow-listen"
  ]
}
